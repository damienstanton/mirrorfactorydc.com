# partially autogenerated by certbot, but pretty vanilla otherwise
server {
    server_name mirrorfactorydc.com www.mirrorfactorydc.com;

    listen 80; # managed by Certbot

    listen 443 ssl; # managed by Certbot
ssl_certificate /etc/letsencrypt/live/mirrorfactorydc.com/fullchain.pem; # managed by Certbot
ssl_certificate_key /etc/letsencrypt/live/mirrorfactorydc.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot



    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    # Redirect non-https traffic to https
    # if ($scheme != "https") {
    #     return 301 https://$host$request_uri;
    # } # managed by Certbot

    location / {
        root /var/www/html/mf;
        index index.html;
    }

    ssl_dhparam /etc/ssl/certs/dhparam.pem;
}
